<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thaethinzarkhaingsoe.myngapaw">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.HomeActivity"></activity>
        <activity android:name=".activity.VolcabularyActivity"></activity>
        <activity android:name=".activity.VocabularyViewActivity"></activity>
        <activity android:name=".activity.GrammarActivity"></activity>
        <activity android:name=".activity.GrammarViewActivity"></activity>
        <activity android:name=".activity.ConversationActivity"></activity>
    </application>

</manifest>